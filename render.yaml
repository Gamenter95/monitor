services:
  - type: worker
    name: crypto-wallet-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: CTOKEN
        sync: false
      - key: ETHERSCAN_API_KEY
        sync: false
      - key: SOLSCAN_API_KEY
        sync: false

### **4. `runtime.txt`** (Specify Python version)
```
python-3.11.9
```

### **5. `.gitignore`**
```
# Environment
.env
venv/
*.pyc
__pycache__/

# Database
*.db
wallets.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
